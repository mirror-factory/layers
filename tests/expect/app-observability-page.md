# Expect flow: app/observability/page.tsx

Open the route represented by app/observability/page.tsx. Verify the Burn tab is the default view, shows daily vendor burn sorted by percent of cap, and includes cap values, alert channel, source, and kill-switch columns. Exercise the AI Calls, Errors, Sessions, Charts, and HTTP tabs, resize to mobile, and check for console errors.
