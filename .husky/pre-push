#!/bin/sh
set -e

# Keep push-time checks fast and deterministic. Browser, visual, eval, and
# release proof now live behind explicit tiers and CI/nightly workflows.
pnpm verify:tier 1

if [ "${RUN_TIER2_ON_PUSH:-}" = "1" ]; then
  pnpm verify:tier 2
fi
